import { Widget } from '@/components/Widget'

export default function Home() {
  return (
    <main className="h-screen flex bg-zinc-50 dark:bg-black items-center justify-center text-zinc-800 dark:text-zinc-50">
      <Widget />
    </main>
  )
}
